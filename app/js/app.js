const checkValue = document.querySelectorAll(".sidebar__label");
const checkbox = document.querySelectorAll(".sidebar__input");

let arr = [];

checkBox.forEach((element) => {
  element.addEventListener("click", (e) => {

  });
});
